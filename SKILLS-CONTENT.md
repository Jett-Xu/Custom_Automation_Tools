# 系統技能總覽 (Skills Content Directory)

這份文件盤點了目前系統內建的所有 AI 技能 (Skills)，並依照適用領域進行分類。每一個技能都具備了「痛點解決方案」、「觸發時機」以及「具體運作方式」。

---

## 🛠️ 軟體工程 (跨領域)

這些技能適用於整個軟體開發生命週期，無論是前端、後端或是維運皆可使用。

### 1. `architect` (系統架構師)
*   **痛點**：接到新需求時，缺乏整體的架構觀與資料夾流向設計，容易導致後續程式碼混亂。
*   **觸發點**：「我要做一個電商系統，幫我規劃專案結構與模組依賴關係。」
*   **運作**：分析需求，產出嚴謹的領域驅動設計 (DDD) 或微服務架構文件與目錄結構建議。

### 2. `task-planner` (任務拆解員)
*   **痛點**：面對龐大的 Feature，不知從何下手，且難以評估工時與步驟。
*   **觸發點**：「這是一個登入系統的需求，幫我拆解成可以執行的子任務清單。」
*   **運作**：將大型需求拆解成多個可被獨立實作的 Ticket/Task，為後續的 `executor` 鋪路。

### 3. `executor` (程式實作員)
*   **痛點**：將任務轉換為具體程式碼的過程繁瑣，且容易手誤。
*   **觸發點**：「根據剛才的拆解任務，幫我實作 `loginService.ts` 的內容。」
*   **運作**：產出實際的程式碼片段 (Snippet)，並存檔至 `output/executor/` 供開發者複製使用。

### 4. `spec-generator` (規格書生成器)
*   **痛點**：需求常常只有一句話或模糊的概念，缺乏明確的規格定義，導致後續開發認知不一致或來回溝通成本高。
*   **觸發點**：「我想要做一個購物車功能，幫我產生 SPEC 規格書。」
*   **運作**：依據給定的需求詳細程度，自動生成高階功能清單或包含 User Story、AC (驗收標準)、資料欄位的詳細 Markdown 規格文件，存檔至 `output/documents/spec/`。

### 5. `sdd-generator` (軟體設計文件生成器)
*   **痛點**：有規格後直接動手寫 Code 容易在架構層面翻車，但手繪架構圖、定義 API 與資料庫 Schema 又非常耗時。
*   **觸發點**：「根據這份購物車的規格，幫我產生 SDD 軟體設計文件。」
*   **運作**：依據需求深度，自動產生技術選型、模組切分，甚至是包含 API 規格、資料表 Schema 與 Mermaid 循序圖的 Markdown 文件，存檔至 `output/documents/sdd/`。

---

## 🎨 前端工程 (Frontend)

這些技能專注於解決前端工程師在切版、測試、狀態模擬等日常工作中的痛點。

### 6. `ui-to-code` (視覺稿轉譯器)
*   **痛點**：將設計稿 (Figma/圖片) 徒手轉換為 HTML/TailwindCSS 耗時費力。
*   **觸發點**：「幫我用 React + Tailwind 切出這個登入畫面。」（可搭配截圖）
*   **運作**：分析版面配置與顏色，產出高品質的前端元件程式碼，存檔至 `output/frontend/ui/`。

### 7. `mock-api-generator` (假資料與 API 模擬器)
*   **痛點**：後端 API 尚未就緒，前端需要手動刻 JSON 或 MSW (Mock Service Worker) 以推進開發。
*   **觸發點**：「幫我產生一個包含 5 筆商品資料的 Mock API JSON。」
*   **運作**：產生符合 REST/GraphQL 規範的 JSON 結構或 MSW Handler 程式碼，存檔至 `output/frontend/mock/`。

### 8. `test-case-writer` (自動化測試撰寫員)
*   **痛點**：手寫單元測試 (Vitest) 或元件測試 (React Testing Library) 枯燥乏味且容易遺漏 Edge Cases。
*   **觸發點**：「幫這份 `Button.tsx` 寫一份完整的元件互動測試。」
*   **運作**：產出覆蓋基本渲染、事件互動與非同步 Mock 的測試程式碼，存檔至 `output/frontend/tests/`。

### 9. `test-strategy-advisor` (測試策略顧問)
*   **痛點**：面對一個全新或龐大的前端專案，不知道該從哪裡開始寫測試，容易盲目追求覆蓋率或寫出極度脆弱的 UI 測試。
*   **觸發點**：「我有一個 React 儀表板專案，請幫我分析裡面有哪些功能需要寫測試，以及該用什麼測試？」
*   **運作**：接收專案路徑或功能清單後，套用「商業價值 vs 邏輯複雜度」矩陣進行全盤掃描。產出一份明確標示「必測、建議測、不要測」的測試策略藍圖與原因，並存檔至 `output/frontend/testing/`。

### 10. `svg-to-component` (SVG 最佳化與元件化)
*   **痛點**：原生 SVG 貼進程式碼中難以維護，且顏色與大小寫死 (hardcoded)。
*   **觸發點**：「把這個 SVG 轉成可透過 Props 控制大小與顏色的 React Icon。」
*   **運作**：移除多餘標籤，並包裝成支援 TypeScript Props 的元件，存檔至 `output/frontend/components/`。

### 11. `storybook-generator` (Storybook 文件生成器)
*   **痛點**：元件寫完後，要手動整理出涵蓋所有 Props 組合的 Storybook 文件相當耗時。
*   **觸發點**：「幫這個 `Card` 元件產生 Storybook 故事檔。」
*   **運作**：讀取元件 Props 型別，自動列舉所有可能狀態並產出 `.stories.tsx`，存檔至 `output/frontend/storybook/`。

### 12. `i18n-translator` (多國語系萃取與翻譯)
*   **痛點**：多國語系需要把硬編碼字串逐一抽出成 JSON 並透過 Google 翻譯，過程繁雜。
*   **觸發點**：「將這個元件裡的中文抽出來，翻譯成英文的 JSON 格式。」
*   **運作**：萃取字串、進行翻譯並產出對應的語系檔，存檔至 `output/frontend/i18n/`。

---

## 📝 綜合輔助 (General)

適用於日常行政、摘要或效率提升的通用工具。

### 13. `todo` (待辦清單小幫手)
*   **痛點**：腦中有許多瑣碎的想法，需要快速結構化並記錄下來。
*   **觸發點**：「幫我記錄一下明天早上要開會、下午要修 Bug。」
*   **運作**：將語音或文字整理成 Markdown 表格，存檔至 `output/todo/`。

### 14. `web_summarizer` (網頁摘要工具)
*   **痛點**：面對長篇大論的網頁或技術文件，缺乏時間閱讀抓重點。
*   **觸發點**：「幫我摘要這個網址的內容重點。」
*   **運作**：讀取外部網頁內容並提煉出核心重點與結論。

### 15. `frontend-architecture-generator` (前端架構生成器)
*   **痛點**：每次開啟新專案或建立新模組時，資料夾結構混亂，缺乏乾淨架構的概念；或是盲目套用殺雞用牛刀的複雜架構。
*   **觸發點**：「我要開一個新的 React 專案，幫我產生標準的目錄結構。」
*   **運作**：首先根據需求評估「專案複雜度」。若是小型專案則給予扁平結構；若是複雜專案，則嚴格依照三層式乾淨架構 (UI -> Hooks -> Services) 自動產出目錄樹與架構說明，存檔至 `output/frontend/architecture/`。
